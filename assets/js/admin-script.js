/**
 * WooCommerce Payment Gateway Boilerplate — Admin Script
 *
 * This file loads ONLY on the WooCommerce Settings > Payments page.
 * Add your admin-side JavaScript logic here.
 *
 * Available globals (passed via wp_localize_script):
 *   window.wcPgBoilerplate.ajaxUrl  — WordPress AJAX URL
 *   window.wcPgBoilerplate.nonce    — Security nonce
 *
 * @package WC_PG_Boilerplate
 */

( function ( $ ) {

    'use strict';

    // TODO: Add your admin JavaScript logic here.
    // Example: toggle fields based on test mode checkbox.

    $( document ).ready( function () {
        // console.log( 'WC PG Boilerplate admin script loaded.' );
    } );

} )( jQuery );
